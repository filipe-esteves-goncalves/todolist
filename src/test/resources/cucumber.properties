
# Try explicit feature paths so the JUnit Platform engine discovers the scenarios.
# We include both the package-based resource and the generic features folder.
cucumber.features=classpath:com/filipe/todolist/health.feature,classpath:features/health.feature
# Include the root test package as glue so Cucumber can find the
# @CucumberContextConfiguration class (`CucumberSpringConfiguration`).
cucumber.glue=com.filipe.todolist,com.filipe.todolist.steps



